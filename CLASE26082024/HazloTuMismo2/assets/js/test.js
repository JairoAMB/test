//Escribe un script que inicialice un arreglo con los números 4, 6, 10.Imprime el arreglo invertido en la consola.
let numbers = [4, 6, 10];

for (let i = numbers.length - 1; i >= 0; i--) {
  console.log(numbers[i]);
}